rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: Allow all reads/writes without authentication
    // WARNING: This is for development/MVP only. Secure before production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}